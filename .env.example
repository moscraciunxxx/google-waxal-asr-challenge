# Optional credentials for WAXAL ASR work.
# Copy to .env and fill in. Never commit .env.

# Hugging Face — optional (models/data already cached for MMS + WaxalNLP).
# Create a read token at https://huggingface.co/settings/tokens
HF_TOKEN=

# Optional experiment tracking
WANDB_API_KEY=

# Do NOT put Zindi passwords here — upload submission.csv in the browser.
