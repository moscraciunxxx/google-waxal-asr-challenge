# Local data

This directory is intentionally empty in the GitHub publication except for
this note. Recreate the public WAXAL tables with:

```bash
python scripts/prepare_data.py
```

Downloaded datasets, Phase-2 audio, metadata caches, language models, and
other derived files are excluded by `.gitignore` because they are large,
generated, or subject to upstream competition-data terms.
