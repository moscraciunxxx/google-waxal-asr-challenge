# Curated public submission

`phase2_public_final.csv` is the latest public Phase-2 candidate prepared for
the expanded 2,392-row portal set. It is structurally validated with
`scripts/validate_public_submission.py`.

The private swing and intermediate experiment files remain local and are not
published here.
